{
  "name": "council-helper-server",
  "version": "0.1.0",
  "private": true,
  "description": "Loopback config helper window for the council skill. Zero runtime dependencies.",
  "license": "PolyForm-Perimeter-1.0.0",
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  }
}
